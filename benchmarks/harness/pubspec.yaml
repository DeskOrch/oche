name: oche_benchmark_harness
description: Reproducible process and load harness for Oche Phase 0.
version: 0.0.1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.0

dependencies:
  crypto: ^3.0.6
  routing_kernel_benchmark: ^0.0.1
